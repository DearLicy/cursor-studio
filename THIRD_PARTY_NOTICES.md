# Third-Party Notices

Cursor Studio uses open-source packages that are declared in `package.json` and locked in `package-lock.json`. Each dependency remains subject to its own license terms.

The source files in this repository are released under the MIT License unless a file states otherwise. Packaged desktop builds include the notices distributed by their respective dependencies.
